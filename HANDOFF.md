# HANDOFF

本 repo 已新增 VS Code workspace 設定於 `.vscode/settings.json`：

```json
{
  "editor.renderWhitespace": "all",
  "diffEditor.ignoreTrimWhitespace": false,
  "files.trimTrailingWhitespace": false
}
```

用途：

- 顯示所有空白字元
- diff 不忽略行尾空白差異
- 儲存時不自動刪除行尾空白
